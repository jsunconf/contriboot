{
  "name": "contriboot",
  "version": "0.0.0",
  "description": "Contributions & Interests",
  "scripts": {
    "start": "parallelshell 'npm run watch' 'http-server static -o'",
    "watch": "npm run clean && parallelshell 'npm run watch:css' 'npm run watch:js' 'npm run watch:jade' 'npm run watch:images'",
    "watch:js": "watchify -v -t [ babelify --presets [ es2015 react ] ] assets/scripts/main.jsx -o static/main.js ",
    "watch:jade": "jade src/*.jade -w --out static ",
    "watch:css": "stylus --inline --watch --sourcemap-inline -u autoprefixer-stylus assets/styles/main.styl -o static",
    "watch:images": "npm run build:images && watch-run -p 'assets/images/*' npm run build:images",
    "clean": "rm -rf static && mkdir static",
    "clean:images": "rm -rf static/images && mkdir static/images",
    "test": "npm run lint && npm run build",
    "lint": "eslint ./assets/scripts",
    "build": "npm run clean && npm run build:css && npm run build:js && npm run build:jade",
    "build:js": "browserify assets/scripts/main.js -t babelify | uglifyjs > static/main.js",
    "build:css": "stylus --inline --compress -u autoprefixer-stylus assets/styles/main.styl -o static",
    "build:jade": "jade src/*.jade --out static ",
    "build:images": "npm run clean:images; cp -r assets/images/* static/images/"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {},
  "devDependencies": {
    "autoprefixer-stylus": "^0.8.1",
    "babel": "^6.3.13",
    "babel-preset-es2015": "^6.3.13",
    "babel-preset-react": "^6.5.0",
    "babelify": "^7.2.0",
    "browserify": "^12.0.1",
    "eslint": "^1.10.3",
    "firebase": "^2.4.0",
    "http-server": "^0.8.5",
    "jade-cli": "^0.1.1",
    "parallelshell": "^2.0.0",
    "react": "^0.14.7",
    "react-dom": "^0.14.7",
    "reactfire": "^0.5.1",
    "stylus": "^0.53.0",
    "stylus-normalize": "^3.0.3",
    "uglify-js": "^2.4.23",
    "watch-run": "^1.2.2",
    "watchify": "^3.2.3"
  }
}
